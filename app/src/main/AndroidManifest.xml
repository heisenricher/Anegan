<?xml version="1.0" encoding="utf-8"?>
<!--
  Copyright (c) 2026 Mahilan (heisenricher). All rights reserved.
  
  This source code is licensed under the custom Anegan Attribution License.
  Any person or entity using, modifying, or building upon this code must
  prominently attribute the original creator Mahilan (heisenricher).
  Personal and educational use only.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Storage permissions for saving and loading files -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Biometric permission for history -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    
    <!-- Internet permission for feedback -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="Anegan"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">

        <!-- Silent copyright credentials metadata -->
        <meta-data
            android:name="com.anegan.copyright"
            android:value="Copyright (c) 2026 Mahilan (heisenricher). All rights reserved. This application and its source code are licensed under the Anegan Attribution License. Uncredited reuse or distribution is strictly prohibited." />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
